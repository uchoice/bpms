<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="net.uchoice.activiti.dao.ActFormDao">
    
	<sql id="actFormColumns">
		a.id AS "id",
		a.name AS "name",
		a.content AS "content",
		a.remarks AS "remarks",
		a.create_by AS "createBy",
		a.create_date AS "createDate",
		a.update_by AS "updateBy",
		a.update_date AS "updateDate"
	</sql>
	
	<sql id="actFormJoins">
	</sql>
    
	<select id="get" resultType="ActForm">
		SELECT 
			<include refid="actFormColumns"/>
		FROM act_form a
		<include refid="actFormJoins"/>
		WHERE a.id = #{id}
	</select>
	
	<select id="findList" resultType="ActForm">
		SELECT 
			<include refid="actFormColumns"/>
		FROM act_form a
		<include refid="actFormJoins"/>
		<where>
			
			<if test="name != null and name != ''">
				AND a.name = #{name}
			</if>
			<if test="remarks != null and remarks != ''">
				AND a.remarks LIKE concat('%',#{remarks},'%')
			</if>
		</where>
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
				ORDER BY a.update_date DESC
			</otherwise>
		</choose>
	</select>
	
	<select id="findFormsByName" resultType="ActForm">
		SELECT 
			<include refid="actFormColumns"/>
		FROM act_form a
		<include refid="actFormJoins"/>
		where a.name in
		<foreach collection="list" item="na" separator="," open="(" close=")">
			#{na}
		</foreach>
		ORDER BY a.update_date DESC
	</select>
	
	<select id="findAllList" resultType="ActForm">
		SELECT 
			<include refid="actFormColumns"/>
		FROM act_form a
		<include refid="actFormJoins"/>
		<where>
			
		</where>		
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
				ORDER BY a.update_date DESC
			</otherwise>
		</choose>
	</select>
	
	<insert id="insert" keyProperty="id" useGeneratedKeys="true">
		INSERT INTO act_form(
			name,
			content,
			remarks,
			create_by,
			create_date,
			update_by,
			update_date
		) VALUES (
			#{name},
			#{content},
			#{remarks},
			#{createBy},
			#{createDate},
			#{updateBy},
			#{updateDate}
		)
	</insert>
	
	<update id="update">
		UPDATE act_form SET 	
			name = #{name},
			content = #{content},
			remarks = #{remarks},
			update_by = #{updateBy},
			update_date = #{updateDate}
		WHERE id = #{id}
	</update>
	
	<update id="delete">
		DELETE FROM act_form
		WHERE id = #{id}
	</update>
	
</mapper>